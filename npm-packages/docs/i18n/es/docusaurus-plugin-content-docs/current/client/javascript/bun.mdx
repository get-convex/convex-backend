---
title: "Bun"
hidden: false
sidebar_position: 200
description: "Usa clientes de Convex con el runtime de JavaScript Bun"
---

[Bun](https://bun.sh/) se puede usar para ejecutar scripts y servidores que usan clientes de
Convex e incluso para ejecutar la CLI de Convex.

Convex admite consultas, mutaciones y acciones puntuales (consulta el
[cliente HTTP](/api/classes/browser.ConvexHttpClient)) y, además, suscripciones de consultas
(consulta [ConvexClient](/api/classes/browser.ConvexClient)) en Bun.

```js
import { ConvexHttpClient, ConvexClient } from "convex/browser";
import { api } from "./convex/_generated/api.js";

// Cliente HTTP
const httpClient = new ConvexHttpClient(process.env.CONVEX_URL);
httpClient.query(api.messages.list).then((messages) => {
  console.log(messages);
});

// Cliente de suscripción
const client = new ConvexClient(process.env.CONVEX_URL);
const unsubscribe = client.onUpdate(api.messages.list, {}, (messages) =>
  console.log(messages),
);
await Bun.sleep(1000);
client.mutate(api.messages.send, {}, { body: "hello!", author: "me" });
await Bun.sleep(1000);
```

## Usar Convex con Bun sin generación de código \{#using-convex-with-bun-without-codegen\}

Siempre puedes usar el objeto `anyApi` o cadenas si no tienes a mano las
funciones de Convex y el archivo de API. Una referencia de API como `api.folder.file.exportName`
se convierte en `anyApi.folder.file.exportName` o `"folder/file:exportName"`.