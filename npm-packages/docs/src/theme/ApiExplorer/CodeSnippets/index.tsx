/*
 * Ejected to disable generated code snippets. Convex documents a typed client
 * instead, so `languageTabs` is empty. Upstream CodeSnippets (5.1.x) crashes on
 * an empty language set (reads `.language` of an undefined language), so
 * rendering nothing both matches intent and sidesteps that crash.
 */

export default function CodeSnippets(): null {
  return null;
}
