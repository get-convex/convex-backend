# Fail installs on unsupported node versions (see "engines" in package.json);
# rush.json's nodeSupportedVersionRange used to enforce this.
engine-strict=true
# Pin the registry so a developer's ~/.npmrc can't redirect workspace installs.
registry=https://registry.npmjs.org/
