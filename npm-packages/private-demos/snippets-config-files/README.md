# Configuration files demo

The `convex/` directory holds the example configuration files embedded in the
"Configuration Files" section of the docs (`npm-packages/docs/docs/config/`).
Keeping them in a real project means `pnpm build` typechecks the examples shown
to users.
