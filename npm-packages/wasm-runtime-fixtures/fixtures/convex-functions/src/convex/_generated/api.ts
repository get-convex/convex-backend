// Stands in for the generated `convex/_generated/api.ts`. The real file exports
// the same `anyApi` proxy, typed against the app's function modules.
import { anyApi } from "convex/server";

export const api = anyApi;
export const internal = anyApi;
